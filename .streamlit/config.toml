[server]
port = 8501
# address não especificado = localhost (para uso local)
# No Railway, será sobrescrito via linha de comando para 0.0.0.0
headless = false  # false para abrir navegador localmente
enableCORS = false
enableXsrfProtection = false

[browser]
gatherUsageStats = false
# serverAddress não especificado = localhost (para uso local)

[theme]
primaryColor = "#1a237e"
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f0f2f6"
textColor = "#262730"
font = "sans serif"

